{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo '[boot] 1/3 migrate' && npx prisma migrate deploy && echo '[boot] 2/3 admin' && (npm run set:admin || echo '[boot] set:admin skipped (무시하고 계속)') && echo '[boot] 3/3 start' && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
