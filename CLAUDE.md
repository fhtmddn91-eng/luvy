# LUVY 작업 지침

성인용품 B2B 도매몰(luvyb2b.com). Next.js 15 + Prisma + Railway/Postgres.
`main` 에 push 하면 자동 배포된다.

## 이미지 번역 — 돈이 나가는 곳

**호출 하나에 얼마인지 알고 눌러라.**

| 호출 | 대략 단가 | 비고 |
|---|---|---|
| 이미지 생성/편집 (`IMAGE_MODEL`) | **~$0.04 / 장** | 비용의 90% 이상이 여기 |
| OCR·문구 번역·검수 (텍스트 모델) | ~$0.0001 | 사실상 공짜 |

### 규칙 1 — 싼 단계에서 막아 비싼 단계를 살린다

문제를 텍스트 단계에서 잡으면 $0.0001, 이미지 단계까지 가면 $0.04 다.
번역문 길이 예산(`charBudget`)이 이 원칙의 예다: 자리에 안 들어갈 문구를
번역할 때 걸러내면, 이미지 생성이 뒷말을 잘라 통째로 다시 뽑는 일이 없다.

### 규칙 2 — 전체 재렌더 금지

카탈로그 전체(248장 ≈ $12)를 다시 돌리는 건 마지막 수단이다. 실제로 하루에
686장을 돌려 크레딧을 소진시킨 적이 있다 — 필요한 건 1회분(191장)이었고
나머지는 "어느 장이 어떤 방식으로 렌더됐는지 기록을 안 남겨서" 매번 전체를
다시 돌린 낭비였다.

고칠 때는 **바뀐 것만** 돌린다. 대상을 못 고르겠으면, 돌리기 전에 고르는
방법부터 만든다.

### 규칙 3 — 대량 작업 전에 한 장으로 검증

프롬프트나 렌더 로직을 바꿨으면 실제 원본 1~4장으로 먼저 돌려 눈으로 본다.
`scratchpad/all/` 에 운영 이미지 109쌍(원본+번역본)과 `meta.json`(저장된
OCR 좌표·문구)이 있어 **API 없이도** 오버레이 경로는 검증할 수 있다.

### 규칙 4 — 지켜지지 않는 규칙은 규칙이 아니다

프롬프트에 규칙을 넣었으면 실제로 지켜지는지 데이터로 확인한다.
`원문 글자수를 넘지 않게` 는 운영 문구 732개 중 **91%가 위반**하고 있었다 —
한국어는 원문보다 길어지는 게 정상(중앙값 1.31배)이라 지킬 수 없는 규칙이었고,
지켜지지 않는 규칙은 모델에게 "여기 규칙은 대충 봐도 된다"고 가르친다.
실측 분포를 보고 지킬 수 있는 수치로 다시 잡아야 한다.

## 번역 파이프라인 구조

```
OCR(extractForeign) → 문구 번역(예산 적용) → 렌더 → 검수 → 사다리
```

**렌더 경로 3단** (`renderTranslatedImage`):
1. 모델 재생성 — 원본+확정 번역문을 주고 나온 그림을 통째로 쓴다. 기본값.
2. 글자 띠 재생성 — 안전 필터가 사진을 보고 거부할 때, 글자 띠만 잘라 재생성.
3. 오버레이 — 우리가 직접 글자를 그린다. 모델 실패 시의 바닥이자 안전망.

**검수** (`flaggedBoxes`): 완성본에 찍힌 글자를 읽어와 대조한다.
- 원문 잔류 (`isForeignSource`)
- 잘림 (`textCoverage` 비율 + `truncatedTail` 앞부분 일치)
- 지우기 경로는 도장 지어내기까지 (`inventedInBox`)

걸리면 재시도 → 걸린 문구만 부분 보정 → 오버레이 순으로 내려간다.
**최악의 경우가 "덧그린 티가 나는 오버레이"**이지, 깨진 이미지가 나가는 길은 없다.

**GIF** 는 프레임마다 그림이 달라져 떨리므로, 첫 프레임만 재생성해 글자 영역을
모든 프레임에 같은 픽셀로 얹는다(정지 패치). 글자 자리가 움직이는 문구만 오버레이.

## 언제 번역이 도는가

- **1688 수집**: 안 돈다. 수집 상품은 숨김으로 들어오고 상당수는 안 팔린다.
- **판매 전환 시**(`setProductStatus` / `updateProduct` 의 ACTIVE): 이때 돈다.
- **국내 도매처**: 전혀 안 돈다. 원문이 이미 한국어다(`sources.ts` 의 `translate: false`).
- **어드민 수동 번역 버튼**: 즉시 돈다.

## 수집(1688·국내 도매처)

새 도매처를 붙일 때 고칠 곳은 `src/lib/import/sources.ts` 하나다 —
host·이미지 호스트 화이트리스트·번역 여부·통화.

이미지 호스트 화이트리스트는 SSRF 방어선이다. payload 는 외부(브라우저)에서
오므로 임의 호스트를 허용하면 서버가 내부망을 긁는 통로가 된다.

같은 원격 이미지는 `StoredFile.sourceUrl` 로 한 번만 받고, 같은 원본이 이미
번역돼 있으면 번역본을 그대로 잇는다(모델 호출 0회).

## 운영 데이터 만질 때

- 운영 DB 접속은 Postgres 서비스의 `DATABASE_PUBLIC_URL` 을 쓴다.
- 이미지 원본은 `ProductAsset.originalUrl` 로 보존된다. 번역본을 갈아끼워도
  되돌릴 수 있다 — 원본 파일은 절대 지우지 않는다.
- 장시간 스크립트는 DB 연결이 끊길 수 있다(3시간쯤에서 겪음). **이어하기가
  가능하게** 짜라 — 어디까지 했는지 알 수 있어야 한다.

## 코드 규약

- 주석은 **왜**를 적는다. 특히 "이렇게 안 하면 무슨 사고가 났는지"(실사례)를 남긴다.
  이 코드의 판정 임계값 대부분은 실제 사고에서 나온 숫자다.
- 순수 함수로 떼어내 단위 테스트로 못 박는다. 되돌아온 버그가 많은 영역이다.
- 임시 스크립트는 `src/lib/__*.test.ts` 로 만들어 `vitest` 로 돌리고(서버 전용
  import 때문에 `tsx` 로는 안 뜬다), 끝나면 지운다.
