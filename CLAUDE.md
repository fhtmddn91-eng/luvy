# LUVY 작업 지침

성인용품 B2B 도매몰(luvyb2b.com). Next.js 15 + Prisma + Railway/Postgres.
`main` 에 push 하면 자동 배포된다.

## 이미지 번역 — 돈이 나가는 곳

**호출 하나에 얼마인지 알고 눌러라.**

| 호출 | 대략 단가 | 비고 |
|---|---|---|
| 이미지 생성/편집 (`IMAGE_MODEL`) | **~$0.067 / 호출** (1K 출력 공식 단가 + 입력 토큰, 추정) | 비용의 90% 이상이 여기 |
| OCR·문구 번역·검수 (텍스트 모델) | ~$0.0001 | 사실상 공짜 |

**자동 흐름의 이미지 API 는 원본당 HTTP 요청 최대 1회다** (2026-08-24 설계 v2.1).
캐시 적중·렌더 전 실패는 0회. 상태 코드(429·5xx·타임아웃)와 무관하게 자동
재요청 금지 — 추가 1회는 어드민의 명시 승인(재렌더 버튼)뿐이다. 같은 원본
바이트는 `TranslationCache`(sha256+pipelineVersion)로 재사용한다 — 모델·프롬프트·
패치·검증 버전이 바뀌면 `translateCache.ts` 의 버전 숫자를 올려라.

### 규칙 1 — 싼 단계에서 막아 비싼 단계를 살린다

문제를 텍스트 단계에서 잡으면 $0.0001, 이미지 단계까지 가면 $0.04 다.
번역문 길이 예산(`charBudget`)이 이 원칙의 예다: 자리에 안 들어갈 문구를
번역할 때 걸러내면, 이미지 생성이 뒷말을 잘라 통째로 다시 뽑는 일이 없다.

### 규칙 2 — 전체 재렌더 금지

카탈로그 전체(248장 ≈ $12)를 다시 돌리는 건 마지막 수단이다. 실제로 하루에
686장을 돌려 크레딧을 소진시킨 적이 있다 — 필요한 건 1회분(191장)이었고
나머지는 "어느 장이 어떤 방식으로 렌더됐는지 기록을 안 남겨서" 매번 전체를
다시 돌린 낭비였다.

고칠 때는 **바뀐 것만** 돌린다. 대상을 못 고르겠으면, 돌리기 전에 고르는
방법부터 만든다.

### 규칙 3 — 대량 작업 전에 한 장으로 검증

프롬프트나 렌더 로직을 바꿨으면 실제 원본 1~4장으로 먼저 돌려 눈으로 본다.
`scratchpad/all/` 에 운영 이미지 109쌍(원본+번역본)과 `meta.json`(저장된
OCR 좌표·문구)이 있어 **API 없이도** 오버레이 경로는 검증할 수 있다.

### 규칙 4 — 지켜지지 않는 규칙은 규칙이 아니다

프롬프트에 규칙을 넣었으면 실제로 지켜지는지 데이터로 확인한다.
`원문 글자수를 넘지 않게` 는 운영 문구 732개 중 **91%가 위반**하고 있었다 —
한국어는 원문보다 길어지는 게 정상(중앙값 1.31배)이라 지킬 수 없는 규칙이었고,
지켜지지 않는 규칙은 모델에게 "여기 규칙은 대충 봐도 된다"고 가르친다.
실측 분포를 보고 지킬 수 있는 수치로 다시 잡아야 한다.

## 번역 파이프라인 구조 (2026-08-24 v2.1 — 자동 1회 + 검증 관문)

```
sha256 캐시 조회 → 교차 OCR(전체+띠, 중복 제거) → 문구 번역(예산)
  → 의미 검수 hard/soft(텍스트, 교정 루프) → 매핑 검사(복제·숫자)
  → 이미지 API 1회(전체 출력 채택) → 내용 기반 완성본 검수
  → 제품 무결성 심사(두 장 비교) → 판정
```

판정(`translateImageAuto` 의 `TranslateOutcome` → `ProductAsset.translateStatus`):
- **VERIFIED / NO_FOREIGN_TEXT** 만 손님용 `url` 로 나간다. legacy(null)도 노출 허용.
- **NEEDS_REVIEW**(잔류·잘림·숫자 변조·새 문구·밀집·OCR 불일치·안전필터…) —
  후보는 `candidateUrl` 로만 보존, `url` 은 원본 유지. 어드민에서 승인/거부/재렌더.
- **RETRYABLE**(타임아웃·429·5xx) — 어드민 "재시도 승인"으로만 1회 재실행.
- 패치 1개 불합격·원문 1건 잔류면 **부분 성공이라도 VERIFIED 금지**.

**자동 정지이미지 렌더는 모델 전체 출력 채택이다** (2026-08-24 전환). 원본 좌표
패치는 모델의 재조판(한국어 길이 차이로 판이 다시 흐름)과 충돌해 live10 실측
자동 통과 0% 였다 — 번역은 정확했는데 좌표 검사가 전량 어긋났다. 그래서 관문을
"픽셀 동일성"에서 **"상품 정보 보존"**으로 옮겼다: 확정 문구 일치·잔류·숫자·
장식은 좌표가 아니라 **글 내용**으로 검사하고, 제품 모습은 원본·완성본 두 장을
비교하는 무결성 심사(PRODUCT_CHANGED)가 지킨다 — 제품 개수·형태·색상·구성
변화는 실격, 판 재배치·글자 차이는 허용. 패치 합성·픽셀 관문(밖 0px·확장 링)은
수동 재렌더·워터마크 지우기·GIF 경로에 그대로 남아 있다. 로컬/인페인팅
(LaMa·MI-GAN) 결과는 고객용 경로에 넣지 않는다(실측: 사진·그라데이션에서 자국).
수동 문구 수정 재렌더(`updateAssetTranslation`)도 결과가 후보로 떠서 육안 승인을
거친다.

**판매 전환 게이트**: `setProductStatus(ACTIVE)` 는 전 이미지가 노출 허용일 때만
즉시 ACTIVE. 아니면 HIDDEN + `publishRequestedAt` 로 보류 → 번역·검증이 끝나면
`promoteIfReady` 가 자동 승격한다. 번역 중 중국어 원본이 손님에게 보이는 창이 없다.

### GIF — 정지 띠 국소 편집 (2026-09-01 v3, 정지 이미지와 다른 길)

프레임마다 모델을 돌리면 그림이 미묘하게 달라져 애니메이션이 떨린다. 그래서
**글자가 모든 프레임에서 완전히 정지인 띠만** 골라 첫 프레임에서 그 띠만 다시
그리고, 같은 픽셀을 모든 프레임에 얹는다. 정지 이미지의 "전체 채택"과 달리
GIF 는 국소 편집이다 — 프레임 밖을 건드리면 애니메이션이 죽는다.

```
정지 띠 고르기 → 겹침 해소 → 이웃 글자 회피 클립 → 커버 검증
  → [띠마다] 재생성(띠 전용 프롬프트) → 잔류·누락·육안·이음매·글자크기 관문
     → 불합격이면 사유별 힌트(실측값 포함)로 1회 재시도
     → 그래도 불합격: 깨진 것은 원문 유지 / 작아진 것만은 더 나은 쪽 채택 + 사유
  → 페더(여백 안쪽·비이음매만) → 전 프레임에 같은 패치 → 중복 프레임 병합
```

**띠를 고르는 규칙** (전부 실사고에서 나온 것):
- 정지 판정은 **흩어진 잡티만** 허용한다: 총량 24px 또는 면적의 1% 중 큰 쪽, 덩어리는
  8px 까지. 예전 "1% 무조건 허용"은 덩어리 움직임까지 통과시켜 전 프레임에 얼어붙었고
  (실측: 라벨 영역 움직임 13.7%→5.4%), 그 뒤 "완전 정지·절대 24px"은 반대로 큰 제목을
  팔레트 흔들림 0.3% 로 떨어뜨렸다(운영 GIF 95장 조사: "움직이는 글자" 47개 중 40개가
  이 경우, 글자 획은 97~100% 정지). 비율 + 덩어리 상한이 둘을 가른다.
- 여백 사다리 60→35→18‰ — 넓을수록 결과가 좋지만 옆 애니메이션을 물어 탈락한다.
- 띠끼리 **겹치면 안 된다**. 겹친 자리에 패치를 두 번 얹어 글자가 두 겹으로
  찍혔다(M19 실측). 합치거나, 글자 사이에서 잘라 나누거나, 하나만 남긴다.
- 띠는 담은 글자를 **전부** 덮어야 한다(+2px 여유). 조각 하나만 쓰던 구버전은
  글자 절반이 패치 밖에 남아 원문이 드러났다(M18 실측: 여백 L−105·B−90).
- 여백에 걸친 **이웃 글자는 잘라낸다**. 모델이 그것까지 손대 헛글자를 만들어
  띠 전체가 버려졌다(M18: 「360°贴合」→"360새름").

**관문 5개** (전부 텍스트 모델·픽셀 계산 = 사실상 공짜):
잔류(한자 남음) · 누락(기대 문구가 판독에 전혀 없음 — 지운 채 비워 둔 띠는 잔류·
헛글자 검사를 다 통과한다) · 육안(겹침·뭉갬·잘림·덧댄 자국·빠짐) · 이음매(경계
색차 + 안쪽 배경 중앙값, **페더 전**에 잰다 — 페더는 경계 1px 을 원본과 섞어
증거를 가린다) · **글자 크기**(결과 글자 높이 ÷ 원문 글자 높이, `bandGlyphShrink`).
깨끗하지 않으면 **원본 유지가 바닥**이다. 덧그리기 폴백은 없앴다.

글자 크기만은 부류가 다르다. 실측(2026-09-02, 산출물 3회분 · 띠 22개): 정상은
88~135%, 작아진 것은 45~83% 였고 「全面覆盖」는 번역이 4자로 짧아도 세 번 다
75~82% 였다 — "원문과 같게"라는 지시를 모델이 절반은 어긴다(규칙 4). 그래서
(1) 프롬프트에 문구별 **글자 높이를 px 로** 적고, (2) 85% 미만이면 실측 비율을
힌트에 실어 재시도하고, (3) 그래도 작으면 **더 나은 쪽을 채택**하되
`GIF_SMALL_TEXT` 로 알린다 — 작은 한국어가 중국어 원문보다는 낫다.

**띠 시도마다 기록을 남긴다** (`[gif 띠 i/n] 「문구」 폭×높이 ×배율 시도 k: 사유`).
실측(마리아 0018)에서 「回弹设计」가 원문으로 남았는데 사유가 비어 있어 관문
불합격인지 호출 실패인지 되짚을 수 없었다 — 기록 없는 실패는 고칠 수 없다.
(그 "빈 사유"의 실체는 검수 사유 표기가 항목을 14자에서 자르던 버그였다 —
`keptOriginalDetail`. 원인을 모른 채 며칠을 보냈다.)

**프롬프트는 띠 전용**(`bandRegenPrompt`)이다. 전체 이미지용을 쓰면 관문이 재는
세 가지(가장자리 배경색·겹침 금지·띠 안에 넣기)를 모델에게 한마디도 말하지 않는
꼴이 된다 — 규칙 1의 응용: 버려질 호출($0.067)보다 통과할 그림을 그리게 하는 쪽이 싸다.

**호출 예산**: 자동 흐름은 원본당 1회이므로 **띠 하나만** 번역된다(나머지는
"이미지 호출 한도 — 원문 유지"). 어드민 「다시 만들기」가 띠별 호출을 허용한다
(상한 6회 = 띠 3개 × 시도 2회, 약 100~600원). GIF 는 항상 NEEDS_REVIEW 다
(`GIF_UNVERIFIED` — 패치 밖 보존을 픽셀로 증명할 수 없어 육안 확인).

**원문을 유지한 문구는 사유와 함께 보고한다**(`GIF_KEPT_ORIGINAL`) — "움직이는
화면 위" / "이음매가 보입니다" 등. 목록만 보면 운영자가 재시도인지 직접
올리기인지 판단할 수 없다.

**번역 길이 예산은 띠 기준이다** (`gifBudgetsFor`): 프레임을 읽어 문구 좌우의
**정지 여백**을 재고(행 범위는 글자 ±8px — 띠는 글자보다 위아래로 두꺼워서, 글자 행만
재면 실측 13 「大头爆震」처럼 띠보다 넓게 세어 글자가 양 끝에 닿는다), 그만큼 넓힌 띠
폭에 원래 글자 높이로 들어가는 글자 수를 예산으로 준다(한국어 한 글자 평균 0.85em,
하한 4자). 원문에 숫자가 없는데 숫자를 나열한 답("1스틱 2용도")은 받지 않는다 —
요청문에 금지 예시로 적어도 두 번 다 냈다(규칙 4). 박스 기준은 양쪽으로
틀렸다 — 실측(exp10): 「人体进阶」 박스로 6자 → "인체 마스터"(어색), 실제 띠는
589px 라 "인체공학 설계"(7자)가 원래 크기로 들어갈 자리였고, 「多种频率」는 여백이
없어 4자("진동모드")가 진실이다. 교정 재번역도 **같은 예산**을 쓴다(느슨한 정지
이미지 예산으로 교정된 18자가 15자 자리로 간 사고). 예산을 넘기면 **직전 답과
초과량을 보여 주며** 2차까지 줄인다 — "넘었으니 줄여라"만으로는 더 짧은 답이
안 왔다. 단 "최대 N자는 상한일 뿐"과 "완결된 명사구로"도 함께 말한다 — 너무
조이면 뜻이 깎이고("실전 자극", 부위 이름 누락) 꼴이 잘린다("여운이 남는").

**soft 불합격**: 글자가 작아진 것과 확정 문구와 어미 정도만 다른 것(유사도 0.8
이상, "자극적이게"↔"자극적으로")은 깨진 것이 아니다. 정확히 맞추려 1회 재시도하고,
못 맞추면 **더 나은 쪽을 채택**한다 — 실측(exp10): 어미 차이를 헛글자로 버렸더니
재시도가 이음매에 걸려 중국어 원문이 남았다. 차이는 최종 관문이 그대로 보고한다.

**띠 여백은 모델에게 숫자로 준다**: 위 1px·아래 12px 같은 자리에서 모델은 여백을
모르고 글자를 위 가장자리에 붙여 그렸다(exp10: 이음매 연속 217px 로 두 번 탈락).
"가장자리에서 3px 이상, 닿느니 줄을 옮기거나 최대 10% 줄여라"를 여백 수치와 함께
말한다. 띠 확장도 막히지 않은 쪽으로 계속 넓힌다(한쪽 움직임에 양쪽이 멈추던
문제 — 「大头爆震」 띠 높이 35→50px).

**다음 실측에는 `GIF_BAND_DEBUG_DIR` 를 준다**: 시도마다 보낸 띠·받은 띠를 PNG 로
남긴다. 관문 사유만으로는 "왜 그렇게 그렸는지"를 볼 수 없다.

**승인 문구가 출발점이다** (`phraseMemory.ts`): 「다시 만들기」와 같은 상품의 다른
그림은 자기 확정 문구(ocrData) → 같은 상품 VERIFIED 문구 → **카탈로그 전체** VERIFIED
문구(상품끼리 갈리면 더 많이 승인된 쪽) 순으로 그대로 쓰고, 띠 예산을 넘는 것만
"직전 답"으로 보여 주며 줄인다. 처음 보는 문구는 **후보 3개**를 받아 텍스트 심사
1회로 고른다(실측 exp12: 하나만 받으면 8개 중 3개가 어색했다).
**재렌더는 판독도 저장본을 쓴다**(`knownBoxes`, 원본 해시가 같을 때): 판독 좌표가
실행마다 몇 px 달라 같은 GIF 도 띠가 생겼다 안 생겼다 했다(exp10 vs exp12). 실측(exp10·11): 재렌더가 매번
처음부터 번역해 승인된 "인체공학 설계"를 "인체 마스터"로 바꿨고, 다음 실행은
「强震蜜豆」 재번역이 의미 검수에 두 번 걸려 이미지 호출 0회로 검수함에 갔다.
번역 모델은 실행마다 다르지만 승인 문구는 사람이 이미 본 것이다. 의미 교정도
GIF 만 2회다(텍스트 호출뿐이라 사실상 공짜). 후보(candidateOcr)는 승인이 아니다.

**패치 밖 보존은 증명할 수 있다**: sharp 의 GIF 재부호화는 원본 프레임에 손실 0 이고,
패치가 색을 보태 256색을 넘는 프레임에서만 양자화가 흩어진다(실측 M18, 415색: 평균
0.14 · 8 넘게 다른 픽셀 0.09% · 최대 22~47). 그래서 프레임마다 패치 밖 **달라진 픽셀
비율**(8 초과, 기준 0.5%)을 재서 `GIF_UNVERIFIED` 사유에 적는다 — 최대값은 몇 픽셀이
튀어 기준이 못 된다. 운영자는 띠 안 글자만 보면 된다. (예전 "픽셀로 증명 불가"는 띠
영역을 섞어 잰 오측이었다.) 인코더는 `interPaletteMaxError: 0` — 기본값(3)은 앞 프레임
팔레트를 재사용해, 띠의 재표본화 회색이 팔레트를 채운 뒤 다음 프레임 전체가 엉뚱한
색(100→78·160→117)에 붙었다.

**글자색·굵기도 관문이 본다** (`bandGlyphColorShift` · `bandGlyphWeightShift`): 문구별
**획 중심색**(가장 진한 30% 의 중앙값)을 전체·왼쪽 반·오른쪽 반에서 원본과 비교해 채널 차
75 를 넘으면 soft(재시도 뒤 채택 + `GIF_TEXT_COLOR`). 굵기는 잉크 픽셀마다 가로·세로
연속 길이의 **짧은 쪽**(획 방향과 무관한 두께)을 평균해 비율 1.4 초과·절대 차 2px 초과,
즉 **굵어지는 쪽만** soft(`GIF_TEXT_WEIGHT`). 재보정 근거(실측 산출물 7회분 대조):
가로 길이만 재면 一 같은 긴 가로획 때문에 「一棒两用」이 0.47 로 잡혔고, 얇아지는 쪽은
한자 굵은 제목→한글 고딕의 서체 차이(0.35~0.66)라 전부 오탐이었다. 진짜 사고(실측 10
부제 1.89 로 굵어짐)는 굵어지는 쪽이다. 모델이 고칠 수 없는 차이를 잡으면 재시도 비용만 나간다.

**관문은 실측 산출물로 재보정한다**: 산출물 7회분(M18 5·M19 2)의 띠를 현재 관문에 통과
시켜 수치 표를 뽑고 정답(실측 12 는 깨끗함·실측 7·9 는 작아짐·M19 「강력 진동」은 밝은
사각형)과 대조한다. 이 방법으로 안쪽 배경 관문의 오탐도 잡았다 — 띠 전체 중앙값은 글자가
30~45% 인 띠에서 글자 쪽으로 넘어가 깨끗한 「全面覆盖」를 5회 전부 "덧댄 자국"으로 냈다.
지금은 원본·합성본 **둘 다 글자가 아닌 픽셀**의 중앙값만 비교한다.
그전엔 빨간 제목이 검정으로 바뀌어도 통과했다. 단순 중앙값은 가는 글자의 번짐 픽셀에
끌려 같은 색을 차 89 로 잡았다(exp12, 재시도 1회 낭비).

**단색 배경 직접 그리기 폴백** (`localSolidPatch`, 2026-09-02 결정): 글자 1px 옆까지
제품 사진이 움직이면 여백 있는 정지 띠가 없다 — 실측(exp10~12 「回弹设计」) 세 번 중 두 번
원문이 남았다. 이런 자리만, 조건이 전부 픽셀로 맞을 때 우리가 직접 쓴다: 잉크 범위(움직이는
픽셀 제외)가 완전 정지, 배경이 단색(글자 아닌 픽셀 95% 가 대표색 ±6), 번역문이 원문 높이의
85% 이상으로 들어감. 배경색으로 칠하고 프리텐다드로 원문 색(획 중심색)·굵기·높이대로
쓴다. 서체는 바뀌지만 자국은 없다 — `GIF_LOCAL_TEXT` 로 알려 운영자가 본다. 사진·
그라데이션(예전 로컬 렌더 자국의 원인)에는 절대 하지 않고, 호출 한도로 미룬 띠에도 하지
않는다(서체 통일 — 「다시 만들기」에서 모델이 그리는 게 낫다).

**돈 안 쓰고 검증하는 법**: 승인된 결과물에서 띠 픽셀을 오려 "모델 응답"으로
되먹이면 띠 선택·관문·페더·합성·프레임 병합을 API 0회로 전 과정 회귀할 수 있다
(fetch 스텁: 이미지 호출은 받은 crop 크기로 띠를 매칭해 승인본에서 오려 주고,
판독 호출은 확정 문구를 그대로 돌려준다). 실제 호출은 마지막 한 장 확인에만 쓴다.

## 언제 번역이 도는가

- **1688 수집**: 안 돈다. 수집 상품은 숨김으로 들어오고 상당수는 안 팔린다.
- **판매 전환 시**(`setProductStatus` / `updateProduct` 의 ACTIVE): 이때 돈다.
- **국내 도매처**: 전혀 안 돈다. 원문이 이미 한국어다(`sources.ts` 의 `translate: false`).
- **어드민 수동 번역 버튼**: 즉시 돈다.

## 수집(1688·국내 도매처)

새 도매처를 붙일 때 고칠 곳은 `src/lib/import/sources.ts` 하나다 —
host·이미지 호스트 화이트리스트·번역 여부·통화.

이미지 호스트 화이트리스트는 SSRF 방어선이다. payload 는 외부(브라우저)에서
오므로 임의 호스트를 허용하면 서버가 내부망을 긁는 통로가 된다.
검사는 `imageUrl.ts` 의 `normalizeImageUrlFor` **하나**로 모은다 — 사이트마다
따로 짜두면 한쪽만 고쳐지고 다른 쪽에 구멍이 남는다.

**북마클릿은 두 벌이다.** 페이지 구조가 달라 파일을 나눴다.

| | 1688 | 국내 도매처 |
|---|---|---|
| 추출 코드 | `bookmarklet.ts` | `bookmarkletDomestic.ts` |
| 내려받는 주소 | `/bookmarklet.js` | `/bookmarklet-domestic.js` |
| 어드민 화면 | `/admin/import` | `/admin/import/domestic` |
| 파서 | `parse1688.ts` | `parseDomestic.ts` |
| 뽑는 방법 | `window.context` 전역 데이터 | JSON-LD → og: → 카페24 셀렉터 → 전체 폴백 |

둘 다 **서버에서 최신 코드를 받아 실행하는 로더**다. 셀렉터를 고쳐 배포하면
등록해 둔 북마크가 그대로 최신이 된다 — 운영자가 다시 드래그할 필요 없다.

국내 사이트 판별표는 `sources.ts` 에서 생성해 북마클릿에 심는다. 손으로 복사해
두면 서버 화이트리스트와 북마클릿이 따로 놀아 "수집은 됐는데 이미지가 한 장도
안 받아지는" 상태가 된다.

`Product.sourceId` 는 전역 유니크다. 쇼핑몰마다 1번부터 번호를 매기므로 국내
도매처는 `사이트id:상품번호` 로 저장한다 — 접두사가 없으면 도라도라 1234번을
수집한 뒤 핑크박스 1234번이 "이미 수집된 상품"으로 거부된다.
(1688 은 offerId 가 전역 고유라 접두사 없이 그대로 쓴다 — 기존 데이터 보존)

같은 원격 이미지는 `StoredFile.sourceUrl` 로 한 번만 받고, 같은 원본이 이미
번역돼 있으면 번역본을 그대로 잇는다(모델 호출 0회).

## 운영 데이터 만질 때

- 운영 DB 접속은 Postgres 서비스의 `DATABASE_PUBLIC_URL` 을 쓴다.
- 이미지 원본은 `ProductAsset.originalUrl` 로 보존된다. 번역본을 갈아끼워도
  되돌릴 수 있다 — 원본 파일은 절대 지우지 않는다.
- 장시간 스크립트는 DB 연결이 끊길 수 있다(3시간쯤에서 겪음). **이어하기가
  가능하게** 짜라 — 어디까지 했는지 알 수 있어야 한다.

## 코드 규약

- 주석은 **왜**를 적는다. 특히 "이렇게 안 하면 무슨 사고가 났는지"(실사례)를 남긴다.
  이 코드의 판정 임계값 대부분은 실제 사고에서 나온 숫자다.
- 순수 함수로 떼어내 단위 테스트로 못 박는다. 되돌아온 버그가 많은 영역이다.
- 임시 스크립트는 `src/lib/__*.test.ts` 로 만들어 `vitest` 로 돌리고(서버 전용
  import 때문에 `tsx` 로는 안 뜬다), 끝나면 지운다.
